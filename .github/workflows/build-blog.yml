name: Build blog

on:
  push:
    branches:
      - master
      - blog

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: master
      - name: Fetch articles from blog branch
        run: |
          git fetch
          git checkout origin/blog -- blog
          ls -l blog/
      - uses: actions/setup-node@v3
        with:
          node-version: '16'
      - name: Scully Publish
        uses: cmgriffing/scully-gh-pages-action@46aece06aede8d1c7b4a6aeeac982e44300912bf
        with:
          access-token: ${{ secrets.ACCESS_TOKEN }}
          deploy-branch: gh-pages
